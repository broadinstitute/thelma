# Note: default versions for every chart are configured in `versions/cluster/dev.yaml`

# Our cluster releases often create/manage cluster-wide resources, and this is outside security boundaries
clusterResourceWhitelist:
  - group: '*'
    kind: '*'

releases:
  install-secrets-manager:
    enabled: true
