# Environment overrides for prod environment

# Cluster in ArgoCD where manifests should be deployed to
defaultCluster: terra-prod

# Require users to be in Workbench Admins group in order to
# be able to make changes to this environment
requireSuitable: true

releases:
  datarepo:
    enabled: true
    cluster: datarepo-production
